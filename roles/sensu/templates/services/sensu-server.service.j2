[Unit]
Description=sensu server

[Service]
User=sensu
Group=sensu
ExecStart=/opt/sensu/bin/sensu-server -c "{{ base.config_dir }}"/conf.d -d "{{ base.config_dir }}"/conf.d -e "{{ base.config_dir }}"/extensions -l /var/log/sensu/sensu-server.log
KillMode=process
Restart=on-failure
RestartSec=1min

[Install]
WantedBy=multi-user.target
